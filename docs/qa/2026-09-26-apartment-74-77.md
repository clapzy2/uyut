# Контрольный план квартиры 74,77 м²

Источник: предоставленный владельцем `apartment_74_77m2_full_project.pdf`, 48 страниц.
Визуально проверены страница 6 / лист 03 «Обмерный план» и отдельно страница 12 / лист 08
«План после перепланировки». Нумерация страниц PDF и листов альбома различается.
Исходный PDF не изменён и в репозиторий не добавлен. После разрешения владельца выполнены
два запроса fal: только изображение страницы 6, во втором также текстовый слой этой страницы.
Полный 48-страничный альбом не отправлялся.
Адрес и сведения из штампа в эталон не копировались.

## Контрольные значения существующего состояния

| № на плане | Помещение | Площадь, м² | Подписанный горизонтальный габарит, мм | Вертикальный габарит, мм |
|---|---|---:|---:|---:|
| 1 | Прихожая | 4,21 | Не размечен | Не размечен |
| 2 | Кухня | 12,35 | 2964 | 4205 |
| 3 | Гостиная | 17,05 | 3389 | 5158 |
| 4 | Спальня | 15,39 | 2985 | 5156 |
| 5 | Коридор | 7,70 | Не размечен | Не размечен |
| 6 | Спальня | 12,23 | 2945 | 4154 |
| 7 | Ванная | 3,89 | 1754 | 2218 |
| 8 | Санузел | 1,95 | 1755 | 1180 |

Сумма подписанных площадей: **74,77 м²**, совпадает с экспликацией. Габариты не являются
доказательством прямоугольного свободного пространства: у кухни есть выступ, у гостиной
скошенная граница, прихожая и коридор требуют контурной разметки. Их размеры не достроены
по площади. «Не размечен» не означает, что на чертеже вообще нет размерных отрезков.

Контроль размерных цепочек: 525 + 563 + 926 + 950 = 2964; 3718 + 487 = 4205;
939 + 1344 + 702 = 2985; 866 + 1353 + 726 = 2945 мм. Радиатор под окном не является
оконным проёмом; у него отдельное обозначение в легенде. Проёмы, радиаторы, шахты и стояки
видны, но их точные координаты и привязки этим эталоном ещё не проверены.

Единого потолка нет: примечание задаёт диапазон 2649–2684 мм; в спальнях №4 и №6
подписаны 2663 и 2672 мм. Балка 2467 мм, входная дверь 2053 мм, балконная дверь 2281 мм,
проём 2079 мм — отдельные подписи, не высота потолка. h1/h2 окон сохранены как исходные
обозначения; смысл h2 не объявлен подтверждённым только по этому листу.

Сам автор плана требует уточнить размеры после выравнивания стен, пола и потолка.
Площади не учитывают пространство под дверными проёмами. Совпадение с этим PDF подтверждает
чтение чертежа, а не точность обмеров на месте или пригодность к закупке мебели.

## Проектное состояние не является исходным

На странице 12 / листе 08 после перепланировки **7 помещений, 74,32 м²**, кухня-гостиная
26,03 м² и детская 16,50 м². В исходном состоянии отдельно кухня и гостиная, две спальни.
На исходном листе выводы радиаторов указаны из пола; на проектном — из стены.
Планы мебели (страница 7), демонтажа (8), монтажа (9) и после перепланировки (12)
нельзя объединять с обмерным листом как дополнительные измерения того же состояния.

## Что действительно проверено бесплатно

Ручной эталон: `docs/qa/fixtures/apartment-74-77.json`. Отдельный скрипт использует
реальный `parseFloorPlan`, но подаёт ручной JSON, **не вызывает зрячую модель**:

```powershell
cd C:\Users\Lenovo\Desktop\Room_project\apps\web
bun run scripts/plan-reference-report.ts
```

Результат на коде `9dc8a01`:

- Сохранены 8 помещений, включая две разные спальни; лишних комнат нет.
- Числа теряют исходную точность: 74,77 становится 74,8 м²; 2964 становится 2960 мм;
  4205 становится 4210 мм. Это нормализация кодом, не ошибка модели.
- При null-сторонах и отсутствующем aspect поля остаются пустыми. Но с демонстрационным
  aspect=1 парсер достраивает прихожую 205×205 см и коридор 277×277 см с estimated.
  Это не размеры этой квартиры: тест специально показывает действие fallback.
- Индивидуальные потолки и существующее/проектное состояние не входят в нынешний
  контракт ответа читателя. Номера помещений также не сохраняются, спальни различаются
  порядком; перестановка ответа модели потребует ручной идентификации.
- `plan-reading.ts` растеризует только первые 3 страницы. Исходный лист на странице 6
  не участвует в обычном чтении этого полного альбома.

С `--answer путь-к-сохранённому-ответу.txt` скрипт сравнит уже сохранённый ответ читателя
с тем же эталоном без нового AI-вызова. Сам факт передачи файла не доказывает происхождение
ответа; для платного прогона отдельно нужны модель, выбранная страница, дата и стоимость.
Статус процесса 0 означает, что отчёт сформирован, не что все сравнения прошли.

Проверки самого QA-инструмента: Biome и typecheck прошли; 92 существующих теста парсера
и строк плана в двух файлах прошли. Проверены ручной режим и чтение файла через --answer
на той же контрольной разметке, без AI. Эти 92 теста не являются 92 испытаниями данного PDF.

## Выполненная ограниченная пачка

1. В форме можно выбрать страницу PDF. Сервер проверяет диапазон, растеризует один лист и
   выполняет один запрос; больше нет скрытых запросов на первые три страницы и до 12 сторон.
2. Парсер сохраняет миллиметры (2964 → 296,4 см), сотые площади и отдельные потолки.
   Null не достраивается из площади/aspect. Кнопки подгонки сторон по площади удалены.
3. Номера из экспликации различают спальни №4 и №6 независимо от порядка ответа. Состояние
   листа, номер страницы и количество страниц сохраняются. Проектный лист имеет предупреждение.
4. PDF-текст извлекается бесплатно с координатами и поворотом подписей; растровые сканы могут
   не иметь такого слоя. Позиции текста — нормализованные координаты страницы, не обмеры стен.
5. Валидация и сохранение передают индивидуальные потолки в комнаты, не создавая общий потолок
   из диапазона. Чтение не создаёт подтверждение фактического замера. Предупреждение о расхождении
   площади не объявляет автоматически ошибку стороны: причиной может быть ниша/скос.

## Два реальных AI-прогона — критерий точности не пройден

Модель: `anthropic/claude-sonnet-4.5`, endpoint `fal-ai/any-llm/vision`, temperature 0,
max_tokens 12000. Одна страница, JPEG 1414×2000, качество 88. Первый запрос 17:09:53 UTC,
второй 17:13:53 UTC 26 сентября. Во второй запрос добавлены 192 нативные подписи PDF с
позициями; значения контрольного эталона в запросы не подмешивались.

| Проверка | Только изображение | Изображение + PDF-текст |
|---|---:|---:|
| Число помещений / номера | 8 / 8 | 8 / 8 |
| Площади помещений точно | 7 / 8 | 8 / 8 |
| Общая площадь и existing | Совпали | Совпали |
| Контрольные габариты точно | 0 / 12 | 7 / 12 |
| Известные стороны пропущены | 9 / 12 | 0 / 12 |
| Проверенные индивидуальные потолки | 0 / 2 | 1 / 2 |

В первом ответе ванная прочитана как 3,99 вместо 3,89 м²; кухня получила 2716 вместо 2964 мм.
Во втором ответе кухня получила соседние 3389×4244 вместо 2964×4205 мм, горизонталь гостиной —
3676 вместо 3389 мм, ванная — 1954×2747 вместо 1754×2218 мм. Потолок 2663 мм попал к гостиной
вместо спальни №4. У прихожей и коридора вернулись четыре размера, **не подтверждённые данным
эталоном**; общую цепочку нельзя приписать одному помещению. Их совпадение с каким-либо числом
в текстовом слое не доказывает правильную привязку к комнате.

Геометрия второго ответа осталась черновиком: после проверок кода 16 стен, 6 проёмов и всего
1 контур комнаты, несколько элементов отброшены. Точность оставшихся стен/проёмов не подтверждена.
Успешный API-ответ и хороший результат площадей не позволяют объявить точное распознавание или 3D.

Отправлены два оплачиваемых запроса без повторов и генераций интерьера. Ориентир по
[схеме и цене fal](https://fal.ai/models/fal-ai/any-llm/vision/api): $0,03 каждый, $0,06 вместе.
**Фактическое списание не подтверждено:** API billing вернул 403 для имеющегося ключа.
Остаток баланса не объявляется известным. Endpoint помечен deprecated на странице провайдера,
хотя эти два запроса выполнены; это эксплуатационный риск, не основание угадывать новый endpoint.

Изображения, сырой ответ и метаданные сохранены локально в игнорируемых каталогах
`output/quality-bench/plan-74-77` и `output/quality-bench/plan-74-77-native-text`. Сверка ответов
повторяется через `plan-reference-report.ts --answer ...` бесплатно. Скрипт платного прогона
отказывается повторять записанную попытку; без `--run-paid` только растеризует PDF.

## Что дальше в этом же 2D-этапе

Бесплатные проверки новой пачки: Biome, typecheck всех 7 пакетов и **736 unit-тестов** в
84 файлах прошли. В них есть регрессии выбора страницы 6/48, недопустимого номера без
AI-вызова, сохранения миллиметров/площадей/потолков, номеров помещений, защиты от смешивания
existing/proposed и отсутствия автоматического достраивания сторон. Это тесты кода,
не 736 реальных планов. Форма проверена статическим рендером, не интерактивным E2E с этим PDF.

Сначала вручную размечаем и сверяем контуры, окна/двери и шахты исходного листа, особенно кухню,
скос гостиной и общий коридор. Затем сравниваем привязки размерных линий, а не только числа,
и добавляем регрессии для чужой размерной цепочки и потолка соседней комнаты. Не подгоняем
эталон под ответы AI. Новые запросы пока остановлены: повтор одинакового полного промпта не
закроет дефект привязки. Полное распознавание не считается прошедшим контроль.

Полноценный 3D не начат. Эти проверки относятся к достоверному входу текущего 2D-этапа,
а не к новому бесконечному перечню функций. Владелец сейчас не должен пополнять баланс.
