-- Вместе с прогоном запоминаем его запуск: по нему видно, готовы ли уже концепты именно
-- этого прогона. Без этого страница не может отличить «ещё не создались» от «давно готовы».
ALTER TABLE "rooms" ADD COLUMN "generation_batch_id" uuid;